# Shorebird Configuration
# This file identifies your app on Shorebird.
# When running with a valid SHOREBIRD_TOKEN in CI, if app_id is unset or YOUR_APP_ID,
# CI will automatically run `shorebird init --force` to link/create the app.
app_id: YOUR_APP_ID
auto_update: true
